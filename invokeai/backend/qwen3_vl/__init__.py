"""Qwen3-VL encoder backend module.

Vendored assets for the Qwen3-VL text encoders that arrive as single-file safetensors or GGUF
containers, which carry weights only. See `qwen3_vl_assets` for what is bundled and why.
"""
